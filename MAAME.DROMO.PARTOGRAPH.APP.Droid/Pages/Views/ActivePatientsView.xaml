<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MAAME.DROMO.PARTOGRAPH.APP.Droid.Pages.Views.ActivePatientsView"
             xmlns:f="clr-namespace:Fonts"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:models="clr-namespace:MAAME.DROMO.PARTOGRAPH.MODEL;assembly=MAAME.DROMO.PARTOGRAPH.MODEL"
             xmlns:pagemodels="clr-namespace:MAAME.DROMO.PARTOGRAPH.APP.Droid.PageModels"
             xmlns:popup="clr-namespace:Syncfusion.Maui.Popup;assembly=Syncfusion.Maui.Popup"
             xmlns:fonts="clr-namespace:Fonts"
             x:Name="PatientsView">

    <Grid RowDefinitions="Auto,*" Padding="20">

        <!-- Search Bar -->
        <SearchBar Grid.Row="0"
                   Placeholder="Search by name or hospital number..."
                   Text="{Binding SearchText}"
                   Margin="0,0,0,20" />

        <!-- Patients List -->
        <RefreshView Grid.Row="1"
                     IsRefreshing="{Binding IsRefreshing}"
                     Command="{Binding RefreshCommand}">
            <CollectionView ItemsSource="{Binding Patients}"
                            ItemsLayout="VerticalList"
                            SelectionMode="None"
                            x:Name="PatientCollectionView">
                <CollectionView.EmptyView>
                    <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="20">
                        <Label Text="&#xE8B6;" 
                               FontFamily="{x:Static fonts:FluentUI.FontFamily}"
                               FontSize="64"
                               TextColor="{AppThemeBinding Light=#CCC, Dark=#666}"
                               HorizontalOptions="Center" />
                        <Label Text="No Active Patients"
                               FontSize="18"
                               TextColor="{AppThemeBinding Light=#666, Dark=#AAA}"
                               HorizontalOptions="Center" />
                    </VerticalStackLayout>
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Patient">
                        <Border StrokeThickness="1"
                                Stroke="{AppThemeBinding Light=#E0E0E0, Dark=#424242}"
                                BackgroundColor="{AppThemeBinding Light=White, Dark=#1E1E1E}"
                                Margin="0,5"
                                Padding="15">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="10" />
                            </Border.StrokeShape>

                            <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="15">
                                <!-- Patient Avatar -->
                                <Border Grid.Column="0"
                                        WidthRequest="60"
                                        HeightRequest="60"
                                        BackgroundColor="{Binding StatusColor}">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="30" />
                                    </Border.StrokeShape>
                                    <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center">
                                        <Label Text="{Binding CervicalDilationOnAdmission, StringFormat='{0}'}"
                                               FontSize="20"
                                               FontAttributes="Bold"
                                               TextColor="White" />
                                        <Label Text="cm"
                                               FontSize="12"
                                               TextColor="White" />
                                    </VerticalStackLayout>
                                </Border>

                                <!-- Patient Details -->
                                <VerticalStackLayout Grid.Column="1" Spacing="5" VerticalOptions="Center">
                                    <Label Text="{Binding Name}" 
                                           FontAttributes="Bold" 
                                           FontSize="18" />
                                    <Label Text="{Binding DisplayInfo}" 
                                           FontSize="14"
                                           TextColor="{AppThemeBinding Light=#666, Dark=#AAA}" />
                                    <HorizontalStackLayout Spacing="10">
                                        <HorizontalStackLayout Spacing="5">
                                            <Label Text="&#xe71e;" 
                                                   FontFamily="MaterialAssets"
                                                   FontSize="16"
                                                   TextColor="#2196F3" />
                                            <Label Text="{Binding LaborStartTime, StringFormat='{0:HH:mm}'}" 
                                                   FontSize="12" />
                                            <Label Text="{Binding LaborStartTimeFormat}" 
                                                   FontSize="12" />                                            
                                        </HorizontalStackLayout>
                                        <HorizontalStackLayout Spacing="5">
                                            <Label Text="FHR:" FontSize="12" />
                                            <Label Text="{Binding PartographEntries[0].FetalHeartRate, StringFormat='{0}bpm'}" 
                                                   FontSize="12"
                                                   FontAttributes="Bold"
                                                   TextColor="{Binding PartographEntries[0].FetalHeartRate, Converter={StaticResource FHRColorConverter}}" />
                                        </HorizontalStackLayout>
                                    </HorizontalStackLayout>
                                </VerticalStackLayout>
                                
                                <!-- Action Buttons -->
                                <Grid Grid.Column="2" RowDefinitions="*,*" RowSpacing="5">
                                    <Button Grid.Row="0"
                                            Text="{x:Static f:FluentUI.note_24_regular}"
                                            FontFamily="{x:Static f:FluentUI.FontFamily}"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type pagemodels:ActivePatientsPageModel}}, Path=NavigateToPartographCommand, x:DataType=pagemodels:ActivePatientsPageModel}"
                                            CommandParameter="{Binding .}"
                                            BackgroundColor="#2196F3"
                                            TextColor="White"
                                            WidthRequest="45"
                                            HeightRequest="45"
                                            CornerRadius="22"
                                            ToolTipProperties.Text="View Partograph" />

                                    <Button Grid.Row="1"
                                            Text="{x:Static f:FluentUI.edit_20_regular}"
                                            FontFamily="{x:Static f:FluentUI.FontFamily}"
                                            BackgroundColor="#4CAF50"
                                            TextColor="White"
                                            WidthRequest="45"
                                            HeightRequest="45"
                                            CornerRadius="22"
                                            ToolTipProperties.Text="Add Entry"
                                            Clicked="OnShowMenuClicked"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>

        <popup:SfPopup Grid.Row="1" x:Name="contextMenuPopup" OverlayMode="Transparent"
                       ShowFooter="False"
                       ShowHeader="False" 
                       WidthRequest="200"
                       HeaderHeight="800"
                       >
            <popup:SfPopup.ContentTemplate>
                <DataTemplate>
                    <StackLayout Padding="5" Spacing="0">

                        <Button Text="Companion" 
                                BackgroundColor="Transparent"
                                TextColor="Black"
                                Clicked="OnShareClicked" HorizontalOptions="StartAndExpand"
                                HeightRequest="35" />

                        <Button Text="Pain Relief" 
                                BackgroundColor="Transparent"
                                TextColor="Black"
                                Clicked="OnShareClicked" HorizontalOptions="StartAndExpand"
                                HeightRequest="35"  />

                        <Button Text="Oral Fluid" 
                                BackgroundColor="Transparent"
                                TextColor="Black"
                                Clicked="OnShareClicked" HorizontalOptions="StartAndExpand"
                                HeightRequest="35"  />

                        <Button Text="Posture" 
                                BackgroundColor="Transparent"
                                TextColor="Black"
                                Clicked="OnShareClicked" HorizontalOptions="StartAndExpand"
                                HeightRequest="35"  />

                        <Button Text="FHR" 
                                BackgroundColor="Transparent"
                                TextColor="Black"
                                Clicked="OnEditClicked" HorizontalOptions="StartAndExpand"
                                HeightRequest="35"  />

                        <Button Text="Amniotic Fluid" 
                                BackgroundColor="Transparent"
                                TextColor="Black"
                                Clicked="OnShareClicked" HorizontalOptions="StartAndExpand"
                                HeightRequest="35"  />

                        <Button Text="Fetail Position" 
                                BackgroundColor="Transparent"
                                TextColor="Black"
                                Clicked="OnShareClicked" HorizontalOptions="StartAndExpand"
                                HeightRequest="35"  />

                        <Button Text="Caput" 
                                BackgroundColor="Transparent"
                                TextColor="Black"
                                Clicked="OnShareClicked" HorizontalOptions="StartAndExpand"
                                HeightRequest="35"  />

                        <Button Text="Moulding" 
                                BackgroundColor="Transparent"
                                TextColor="Black"
                                Clicked="OnShareClicked" HorizontalOptions="StartAndExpand"
                                HeightRequest="35"  />

                        <Button Text="Urine" 
                                BackgroundColor="Transparent"
                                TextColor="Black"
                                Clicked="OnShareClicked" HorizontalOptions="StartAndExpand"
                                HeightRequest="35"  />

                        <Button Text="Blood pressure" 
                                BackgroundColor="Transparent"
                                TextColor="Black"
                                Clicked="OnShareClicked" HorizontalOptions="StartAndExpand"
                                HeightRequest="35"  />

                        <Button Text="Contraction" 
                                BackgroundColor="Transparent"
                                TextColor="Red"
                                Clicked="OnDeleteClicked" HorizontalOptions="StartAndExpand"
                                HeightRequest="35"  />
                        
                        <Button Text="Head Descent" 
                                BackgroundColor="Transparent"
                                TextColor="Black"
                                Clicked="OnShareClicked" 
                                HorizontalOptions="StartAndExpand"
                                HeightRequest="35"  />

                    </StackLayout>
                </DataTemplate>
            </popup:SfPopup.ContentTemplate>
        </popup:SfPopup>
    </Grid>
</ContentView>
