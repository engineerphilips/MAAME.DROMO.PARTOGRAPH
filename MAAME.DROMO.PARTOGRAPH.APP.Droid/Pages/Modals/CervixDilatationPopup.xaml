<?xml version="1.0" encoding="utf-8" ?>
<sfPopup:SfPopup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sfPopup="clr-namespace:Syncfusion.Maui.Popup;assembly=Syncfusion.Maui.Popup"
             xmlns:core="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
             xmlns:editors="clr-namespace:Syncfusion.Maui.Inputs;assembly=Syncfusion.Maui.Inputs"
             x:Class="MAAME.DROMO.PARTOGRAPH.APP.Droid.Pages.Modals.CervixDilatationPopup">

    <sfPopup:SfPopup.Resources>
        <ResourceDictionary>
            <!-- Input Styles -->
            <Style x:Key="PatientInput" TargetType="core:SfTextInputLayout">
                <Setter Property="ContainerType" Value="Outlined" />
                <Setter Property="OutlineCornerRadius" Value="8" />
            </Style>
        </ResourceDictionary>
    </sfPopup:SfPopup.Resources>
    <sfPopup:SfPopup.HeaderTemplate>
        <DataTemplate>
            <StackLayout>
                <Label Text="Cervix Dilatation" VerticalOptions="Center" Margin="15,15,0,0" />
            </StackLayout>
        </DataTemplate>
    </sfPopup:SfPopup.HeaderTemplate>
    <sfPopup:SfPopup.ContentTemplate>
        <DataTemplate>
            <ScrollView>
                <VerticalStackLayout Padding="15,10" Spacing="10">
                    <core:SfTextInputLayout Hint="Date"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Recording time">
                        <DatePicker Date="{Binding RecordingDate, Mode=TwoWay}" />
                    </core:SfTextInputLayout>

                    <core:SfTextInputLayout Hint="Time"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Recording time">
                        <TimePicker Time="{Binding RecordingTime, Mode=TwoWay}" />
                    </core:SfTextInputLayout>

                    <core:SfTextInputLayout Hint="Cervix Dilatation (cm)"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Dilatation 0-10 cm">
                        <editors:SfNumericEntry Value="{Binding Dilatation, Mode=TwoWay}"
                                               Minimum="0" Maximum="10"
                                               CustomFormat="0"
                                               ValueChangeMode="OnKeyFocus" />
                    </core:SfTextInputLayout>

                    <!-- Advanced Fields Toggle -->
                    <Frame BorderColor="{AppThemeBinding Light=#E0E0E0, Dark=#424242}"
                           BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#2C2C2C}"
                           CornerRadius="10"
                           Padding="15,10"
                           Margin="0,10,0,10">
                        <HorizontalStackLayout Spacing="10">
                            <Switch IsToggled="{Binding ShowAdvancedFields}"
                                    VerticalOptions="Center"
                                    OnColor="#4CAF50" />
                            <Label Text="Show Advanced WHO 2020 Fields"
                                   VerticalOptions="Center"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light=#333, Dark=White}" />
                        </HorizontalStackLayout>
                    </Frame>

                    <!-- WHO 2020 Advanced Fields Section -->
                    <VerticalStackLayout IsVisible="{Binding ShowAdvancedFields}" Spacing="10">

                        <!-- WHO 2020 Critical Labor Progress Assessment -->
                        <Label Text="Cervical Assessment" FontAttributes="Bold" Margin="0,10,0,5" />

                    <core:SfTextInputLayout Hint="Effacement (%)"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Cervical effacement 0-100%">
                        <editors:SfNumericEntry Value="{Binding EffacementPercent, Mode=TwoWay}"
                                               Minimum="0" Maximum="100"
                                               CustomFormat="0"
                                               ValueChangeMode="OnKeyFocus" />
                    </core:SfTextInputLayout>

                    <!-- Partograph Alert Lines -->
                    <Label Text="Partograph Monitoring" FontAttributes="Bold" Margin="0,10,0,5" />

                    <CheckBox IsChecked="{Binding CrossedAlertLine}" />
                    <Label Text="⚠️ Crossed Alert Line" Margin="-30,0,0,0" VerticalOptions="Center" TextColor="Orange" />

                    <CheckBox IsChecked="{Binding CrossedActionLine}" />
                    <Label Text="⚠️ Crossed Action Line - Intervention Needed" Margin="-30,0,0,0" VerticalOptions="Center" TextColor="Red" FontAttributes="Bold" />

                    <!-- Labor Dystocia Alerts -->
                    <Label Text="Labor Progress Alerts" FontAttributes="Bold" Margin="0,10,0,5" />

                    <CheckBox IsChecked="{Binding ProlongedLatentPhase}" />
                    <Label Text="Prolonged Latent Phase" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding ProtractedActivePhase}" />
                    <Label Text="⚠️ Protracted Active Phase (<1 cm/hour)" Margin="-30,0,0,0" VerticalOptions="Center" TextColor="OrangeRed" />

                    <CheckBox IsChecked="{Binding ArrestedDilatation}" />
                    <Label Text="⚠️ Arrested Dilatation (No progress for 4 hours)" Margin="-30,0,0,0" VerticalOptions="Center" TextColor="Red" />

                    </VerticalStackLayout>
                    <!-- End Advanced Fields Section -->

                    <core:SfTextInputLayout Hint="Additional Notes"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Any other observations...">
                        <Entry Text="{Binding Notes, Mode=TwoWay}"
                           Keyboard="Text"
                           ReturnType="Next" />
                    </core:SfTextInputLayout>

                    <!-- Action Buttons -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="15" Margin="0,20,0,40">
                        <Button Grid.Column="0"
                            Text="Cancel"
                            Command="{Binding CancelCommand}"
                            BackgroundColor="{AppThemeBinding Light=#E0E0E0, Dark=#424242}"
                            TextColor="{AppThemeBinding Light=#333, Dark=White}"
                            HeightRequest="50"
                            CornerRadius="10" />

                        <Button Grid.Column="1"
                            Text="Save Assessment"
                            Command="{Binding SaveCommand}"
                            BackgroundColor="#4CAF50"
                            TextColor="White"
                            HeightRequest="50"
                            CornerRadius="10" />
                    </Grid>

                </VerticalStackLayout>
            </ScrollView>
        </DataTemplate>
    </sfPopup:SfPopup.ContentTemplate>

</sfPopup:SfPopup>
