<?xml version="1.0" encoding="utf-8" ?>
<sfPopup:SfPopup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sfPopup="clr-namespace:Syncfusion.Maui.Popup;assembly=Syncfusion.Maui.Popup"
             xmlns:core="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
             xmlns:buttons="clr-namespace:Syncfusion.Maui.Buttons;assembly=Syncfusion.Maui.Buttons"
             x:Class="MAAME.DROMO.PARTOGRAPH.APP.Droid.Pages.Modals.AmnioticFluidPopup">

    <sfPopup:SfPopup.Resources>
        <ResourceDictionary>
            <!-- Input Styles -->
            <Style x:Key="PatientInput" TargetType="core:SfTextInputLayout">
                <Setter Property="ContainerType" Value="Outlined" />
                <Setter Property="OutlineCornerRadius" Value="8" />
            </Style>
        </ResourceDictionary>
    </sfPopup:SfPopup.Resources>
    <sfPopup:SfPopup.HeaderTemplate>
        <DataTemplate>
            <StackLayout>
                <Label Text="Amniotic Fluid" VerticalOptions="Center" Margin="15,15,0,0" />
            </StackLayout>
        </DataTemplate>
    </sfPopup:SfPopup.HeaderTemplate>
    <sfPopup:SfPopup.ContentTemplate>
        <DataTemplate>
            <ScrollView>
                <VerticalStackLayout Padding="15,10" Spacing="10">
                    <core:SfTextInputLayout Hint="Date"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Recording time">
                        <DatePicker Date="{Binding RecordingDate}" />
                    </core:SfTextInputLayout>

                    <core:SfTextInputLayout Hint="Time"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Recording time">
                        <TimePicker Time="{Binding RecordingTime}" />
                    </core:SfTextInputLayout>

                    <VerticalStackLayout Padding="10,0,0,0">
                        <Label Text="What is the state of the amniotic fluid?" FontAttributes="Bold"/>

                        <buttons:SfSegmentedControl x:Name="segmentedControl" CornerRadius="8" Margin="-10,0,0,0" VisibleSegmentsCount="4" SelectedIndex="{Binding AmnioticFluidIndex, Mode=TwoWay}">
                            <buttons:SfSegmentedControl.ItemsSource>
                                <x:Array Type="{x:Type x:String}">
                                    <x:String>Intact</x:String>
                                    <x:String>Clear</x:String>
                                    <x:String>Meconium-stained</x:String>
                                    <x:String>Blood-stained</x:String>
                                </x:Array>
                            </buttons:SfSegmentedControl.ItemsSource>
                        </buttons:SfSegmentedControl>
                    </VerticalStackLayout>

                    <!-- WHO 2020 Critical Meconium Assessment -->
                    <Label Text="Meconium Assessment" FontAttributes="Bold" Margin="0,10,0,5" />

                    <CheckBox IsChecked="{Binding MeconiumStaining}" />
                    <Label Text="Meconium Staining Present" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding MeconiumThickParticulate}" />
                    <Label Text="⚠️ Thick/Particulate Meconium (Grade 2-3)" Margin="-30,0,0,0" VerticalOptions="Center" TextColor="OrangeRed" />

                    <CheckBox IsChecked="{Binding NeonatalTeamAlerted}" />
                    <Label Text="Neonatal Team Alerted" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <!-- Chorioamnionitis Risk -->
                    <Label Text="Chorioamnionitis Risk" FontAttributes="Bold" Margin="0,10,0,5" />

                    <CheckBox IsChecked="{Binding ProlongedRupture}" />
                    <Label Text="Prolonged Membrane Rupture (>18-24 hours)" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding MaternalFever}" />
                    <Label Text="Maternal Fever" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding MaternalTachycardia}" />
                    <Label Text="Maternal Tachycardia" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding FetalTachycardia}" />
                    <Label Text="Fetal Tachycardia" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding UterineTenderness}" />
                    <Label Text="Uterine Tenderness" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <!-- Cord Complications -->
                    <Label Text="Cord Complications" FontAttributes="Bold" Margin="0,10,0,5" />

                    <CheckBox IsChecked="{Binding CordProlapse}" />
                    <Label Text="⚠️ CORD PROLAPSE - EMERGENCY" Margin="-30,0,0,0" VerticalOptions="Center" TextColor="Red" FontAttributes="Bold" />

                    <CheckBox IsChecked="{Binding CordPresentation}" />
                    <Label Text="Cord Presentation" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <core:SfTextInputLayout Hint="Additional Notes"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Any other observations...">
                        <Entry Text="{Binding Notes}"
                           Keyboard="Text"
                           ReturnType="Next" />
                    </core:SfTextInputLayout>

                    <!-- Action Buttons -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="15" Margin="0,20,0,40">
                        <Button Grid.Column="0"
                            Text="Cancel"
                            Command="{Binding CancelCommand}"
                            BackgroundColor="{AppThemeBinding Light=#E0E0E0, Dark=#424242}"
                            TextColor="{AppThemeBinding Light=#333, Dark=White}"
                            HeightRequest="50"
                            CornerRadius="10" />

                        <Button Grid.Column="1"
                            Text="Save Assessment"
                            Command="{Binding SaveCommand}"
                            BackgroundColor="#4CAF50"
                            TextColor="White"
                            HeightRequest="50"
                            CornerRadius="10" />
                    </Grid>

                </VerticalStackLayout>
            </ScrollView>
        </DataTemplate>
    </sfPopup:SfPopup.ContentTemplate>

</sfPopup:SfPopup>
