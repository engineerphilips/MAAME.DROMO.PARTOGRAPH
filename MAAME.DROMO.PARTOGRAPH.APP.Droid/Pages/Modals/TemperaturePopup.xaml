<?xml version="1.0" encoding="utf-8" ?>
<sfPopup:SfPopup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sfPopup="clr-namespace:Syncfusion.Maui.Popup;assembly=Syncfusion.Maui.Popup"
             xmlns:core="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
             xmlns:editors="clr-namespace:Syncfusion.Maui.Inputs;assembly=Syncfusion.Maui.Inputs"
             x:Class="MAAME.DROMO.PARTOGRAPH.APP.Droid.Pages.Modals.TemperaturePopup">

    <sfPopup:SfPopup.Resources>
        <ResourceDictionary>
            <!-- Input Styles -->
            <Style x:Key="PatientInput" TargetType="core:SfTextInputLayout">
                <Setter Property="ContainerType" Value="Outlined" />
                <Setter Property="OutlineCornerRadius" Value="8" />
            </Style>
        </ResourceDictionary>
    </sfPopup:SfPopup.Resources>
    <sfPopup:SfPopup.HeaderTemplate>
        <DataTemplate>
            <StackLayout>
                <Label Text="Temperature" VerticalOptions="Center" Margin="15,15,0,0" />
            </StackLayout>
        </DataTemplate>
    </sfPopup:SfPopup.HeaderTemplate>
    <sfPopup:SfPopup.ContentTemplate>
        <DataTemplate>
            <ScrollView>
                <VerticalStackLayout Padding="15,10" Spacing="10">
                    <core:SfTextInputLayout Hint="Date"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Recording time">
                        <DatePicker Date="{Binding RecordingDate}" />
                    </core:SfTextInputLayout>

                    <core:SfTextInputLayout Hint="Time"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Recording time">
                        <TimePicker Time="{Binding RecordingTime}" />
                    </core:SfTextInputLayout>

                    <core:SfTextInputLayout Hint="Temperature (°C)"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Recording temperature">
                        <editors:SfNumericEntry Value="{Binding TemperatureCelsius, Mode=TwoWay}" Minimum="0"
                                               CustomFormat="0.0"
                                               ValueChangeMode="OnKeyFocus" />
                    </core:SfTextInputLayout>

                    <!-- WHO 2020 Critical Infection Screening -->
                    <Label Text="Infection Risk Assessment" FontAttributes="Bold" Margin="0,10,0,5" />

                    <CheckBox IsChecked="{Binding IntrapartumFever}" />
                    <Label Text="⚠️ Intrapartum Fever (≥38.0°C)" Margin="-30,0,0,0" VerticalOptions="Center" TextColor="OrangeRed" />

                    <CheckBox IsChecked="{Binding ChoriamnionitisRisk}" />
                    <Label Text="Chorioamnionitis Risk" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding ProlongedRupture}" />
                    <Label Text="Prolonged Membrane Rupture (>18-24 hours)" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding MaternalTachycardia}" />
                    <Label Text="Maternal Tachycardia (>100 bpm)" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding FetalTachycardia}" />
                    <Label Text="Fetal Tachycardia (>160 bpm)" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding UterineTenderness}" />
                    <Label Text="Uterine Tenderness" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding OffensiveLiquor}" />
                    <Label Text="Offensive/Foul-Smelling Amniotic Fluid" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <!-- Sepsis Screening -->
                    <Label Text="Sepsis Screening (qSOFA)" FontAttributes="Bold" Margin="0,10,0,5" />

                    <CheckBox IsChecked="{Binding SepsisScreeningDone}" />
                    <Label Text="Sepsis Screening Performed" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding QSOFAPositive}" />
                    <Label Text="⚠️ qSOFA Positive (≥2 criteria)" Margin="-30,0,0,0" VerticalOptions="Center" TextColor="Red" />

                    <!-- Clinical Response -->
                    <Label Text="Clinical Response" FontAttributes="Bold" Margin="0,10,0,5" />

                    <CheckBox IsChecked="{Binding AntipyreticsGiven}" />
                    <Label Text="Antipyretics Given" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding AntibioticsStarted}" />
                    <Label Text="Antibiotics Started" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding CulturesObtained}" />
                    <Label Text="Blood/Urine Cultures Obtained" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <core:SfTextInputLayout Hint="Additional Notes"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Any other observations...">
                        <Entry Text="{Binding Notes}"
                           Keyboard="Text"
                           ReturnType="Next" />
                    </core:SfTextInputLayout>

                    <!-- Action Buttons -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="15" Margin="0,20,0,40">
                        <Button Grid.Column="0"
                            Text="Cancel"
                            Command="{Binding CancelCommand}"
                            BackgroundColor="{AppThemeBinding Light=#E0E0E0, Dark=#424242}"
                            TextColor="{AppThemeBinding Light=#333, Dark=White}"
                            HeightRequest="50"
                            CornerRadius="10" />

                        <Button Grid.Column="1"
                            Text="Save Assessment"
                            Command="{Binding SaveCommand}"
                            BackgroundColor="#4CAF50"
                            TextColor="White"
                            HeightRequest="50"
                            CornerRadius="10" />
                    </Grid>

                </VerticalStackLayout>
            </ScrollView>
        </DataTemplate>
    </sfPopup:SfPopup.ContentTemplate>

</sfPopup:SfPopup>
