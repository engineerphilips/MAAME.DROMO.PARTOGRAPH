<?xml version="1.0" encoding="utf-8" ?>
<sfPopup:SfPopup AnimationMode='SlideOnTop' xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sfPopup="clr-namespace:Syncfusion.Maui.Popup;assembly=Syncfusion.Maui.Popup"
             xmlns:core="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
             xmlns:buttons="clr-namespace:Syncfusion.Maui.Buttons;assembly=Syncfusion.Maui.Buttons"
             x:Class="MAAME.DROMO.PARTOGRAPH.APP.Droid.Pages.Modals.PosturePopup">

    <sfPopup:SfPopup.Resources>
        <ResourceDictionary>
            <!-- Input Styles -->
            <Style x:Key="PatientInput" TargetType="core:SfTextInputLayout">
                <Setter Property="ContainerType" Value="Outlined" />
                <Setter Property="OutlineCornerRadius" Value="8" />
            </Style>
        </ResourceDictionary>
    </sfPopup:SfPopup.Resources>
    <sfPopup:SfPopup.HeaderTemplate>
        <DataTemplate>
            <StackLayout>
                <Label Text="Posture" VerticalOptions="Center" Margin="15,15,0,0" />
            </StackLayout>
        </DataTemplate>
    </sfPopup:SfPopup.HeaderTemplate>
    <sfPopup:SfPopup.ContentTemplate>
        <DataTemplate>
            <ScrollView>
                <VerticalStackLayout Padding="15" Spacing="10">
                    <core:SfTextInputLayout Hint="Date"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Recording time">
                        <DatePicker Date="{Binding RecordingDate}" />
                    </core:SfTextInputLayout>

                    <core:SfTextInputLayout Hint="Time"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Recording time">
                        <TimePicker Time="{Binding RecordingTime}" />
                    </core:SfTextInputLayout>

                    <VerticalStackLayout Padding="10,0,0,0">
                        <Label Text="What is the mother posture?" FontAttributes="Bold"/>

                        <buttons:SfSegmentedControl x:Name="segmentedControl" CornerRadius="8" Margin="-10,0,0,0" VisibleSegmentsCount="2" SelectedIndex="{Binding PostureIndex, Mode=TwoWay}">
                            <buttons:SfSegmentedControl.ItemsSource>
                                <x:Array Type="{x:Type x:String}">
                                    <x:String>Supine</x:String>
                                    <x:String>Upright</x:String>
                                    <x:String>Sitting</x:String>
                                    <x:String>LeftLateral</x:String>
                                    <x:String>Mobile</x:String>
                                    <x:String>Walking</x:String>
                                    <x:String>RightLateral</x:String> 
                                    <x:String>HandsAndKnees</x:String>
                                    <x:String>Squatting</x:String>
                                    <x:String>BirthingBall</x:String>
                                    <x:String>WaterImmersion</x:String>
                                </x:Array>
                            </buttons:SfSegmentedControl.ItemsSource>
                        </buttons:SfSegmentedControl>
                    </VerticalStackLayout>

                    <core:SfTextInputLayout Hint="Additional Notes"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Any other observations...">
                        <Entry Text="{Binding Notes}"
                           Keyboard="Text"
                           ReturnType="Next" />
                    </core:SfTextInputLayout>

                    <!-- WHO 2020 Enhanced Assessment -->
                    <!--<Label Text="WHO 2020 Detailed Assessment" FontAttributes="Bold" FontSize="16" Margin="0,15,0,5"/>

                    <core:SfTextInputLayout Hint="Posture Category" Style="{StaticResource PatientInput}">
                        <Entry Text="{Binding PostureCategory}" Keyboard="Text" />
                    </core:SfTextInputLayout>

                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <core:SfTextInputLayout Grid.Column="0" Hint="Start Time" Style="{StaticResource PatientInput}">
                            <DatePicker Date="{Binding StartTime}" />
                        </core:SfTextInputLayout>
                        <core:SfTextInputLayout Grid.Column="1" Hint="End Time" Style="{StaticResource PatientInput}">
                            <DatePicker Date="{Binding EndTime}" />
                        </core:SfTextInputLayout>
                    </Grid>

                    <core:SfTextInputLayout Hint="Duration (Minutes)" Style="{StaticResource PatientInput}">
                        <Entry Text="{Binding DurationMinutes}" Keyboard="Numeric" />
                    </core:SfTextInputLayout>

                    <core:SfTextInputLayout Hint="Reason" Style="{StaticResource PatientInput}">
                        <Entry Text="{Binding Reason}" Keyboard="Text" />
                    </core:SfTextInputLayout>

                    <Label Text="Effects" FontAttributes="Bold" Margin="0,10,0,5"/>
                    <core:SfTextInputLayout Hint="Effect on Labor" Style="{StaticResource PatientInput}">
                        <Entry Text="{Binding EffectOnLabor}" Keyboard="Text" />
                    </core:SfTextInputLayout>
                    <core:SfTextInputLayout Hint="Effect on Pain" Style="{StaticResource PatientInput}">
                        <Entry Text="{Binding EffectOnPain}" Keyboard="Text" />
                    </core:SfTextInputLayout>
                    <core:SfTextInputLayout Hint="Effect on Contractions" Style="{StaticResource PatientInput}">
                        <Entry Text="{Binding EffectOnContractions}" Keyboard="Text" />
                    </core:SfTextInputLayout>

                    <Label Text="Assessment" FontAttributes="Bold" Margin="0,10,0,5"/>
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10" RowDefinitions="Auto,Auto,Auto" RowSpacing="10">
                        <VerticalStackLayout Grid.Row="0" Grid.Column="0">
                            <Label Text="Patient Choice"/>
                            <Switch IsToggled="{Binding PatientChoice}" HorizontalOptions="Start"/>
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Row="0" Grid.Column="1">
                            <Label Text="Medically Indicated"/>
                            <Switch IsToggled="{Binding MedicallyIndicated}" HorizontalOptions="Start"/>
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Row="1" Grid.Column="0">
                            <Label Text="Mobile and Active"/>
                            <Switch IsToggled="{Binding MobileAndActive}" HorizontalOptions="Start"/>
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Row="1" Grid.Column="1">
                            <Label Text="Restricted Mobility"/>
                            <Switch IsToggled="{Binding RestrictedMobility}" HorizontalOptions="Start"/>
                        </VerticalStackLayout>
                    </Grid>

                    <core:SfTextInputLayout Hint="Mobility Restriction" Style="{StaticResource PatientInput}">
                        <Entry Text="{Binding MobilityRestriction}" Keyboard="Text" />
                    </core:SfTextInputLayout>

                    <core:SfTextInputLayout Hint="Support Equipment" Style="{StaticResource PatientInput}">
                        <Entry Text="{Binding SupportEquipment}" Keyboard="Text" />
                    </core:SfTextInputLayout>

                    <core:SfTextInputLayout Hint="Clinical Alert" Style="{StaticResource PatientInput}" ContainerBackground="LightYellow">
                        <Entry Text="{Binding ClinicalAlert}" Keyboard="Text" />
                    </core:SfTextInputLayout>-->

                    <!-- Action Buttons -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <Button Text="Cancel"
                    Command="{Binding CancelCommand}"
                    BackgroundColor="#9E9E9E"
                    TextColor="White"
                    Grid.Column="0"/>

                        <Button Text="Save"
                    Command="{Binding SaveCommand}"
                    BackgroundColor="#4CAF50"
                    TextColor="White"
                    Grid.Column="1"/>
                    </Grid>

                </VerticalStackLayout>
            </ScrollView>
        </DataTemplate>
    </sfPopup:SfPopup.ContentTemplate>

</sfPopup:SfPopup>

