<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MAAME.DROMO.PARTOGRAPH.APP.Droid.Pages.Modals.FHRContractionModalView"
             xmlns:core="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
             xmlns:buttons="clr-namespace:Syncfusion.Maui.Buttons;assembly=Syncfusion.Maui.Buttons"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:fonts="clr-namespace:Fonts"
             xmlns:editors="clr-namespace:Syncfusion.Maui.Inputs;assembly=Syncfusion.Maui.Inputs">

    <ContentView.Resources>
        <ResourceDictionary>

            <!-- Input Styles -->
            <Style x:Key="PatientInput" TargetType="core:SfTextInputLayout">
                <Setter Property="ContainerType" Value="Outlined" />
                <Setter Property="OutlineCornerRadius" Value="8" />
                <!--<Setter Property="Stroke" Value="{StaticResource BorderLight}" />-->
                <!--<Setter Property="FocusedStroke" Value="{StaticResource PrimaryBlue}" />-->
                <!--<Setter Property="ContainerBackground" Value="{StaticResource SurfaceWhite}" />-->
                <!--<Setter Property="Margin" Value="0,4" />-->
            </Style>
        </ResourceDictionary>
    </ContentView.Resources>

    <!--<ContentView.Behaviors>
        <toolkit:StatusBarBehavior StatusBarColor="{AppThemeBinding Light=#2196F3, Dark=#1565C0}" />
    </ContentView.Behaviors>-->

    <ScrollView>
        <VerticalStackLayout Padding="15,10" Spacing="10">

            <!-- Header -->
            <!-- Pain Level Assessment -->

            <core:SfTextInputLayout Hint="Date"
                                            Style="{StaticResource PatientInput}"
                                            HelperText="Recording time">
                <DatePicker Date="{Binding RecordingDate, Converter={StaticResource DateOnlyToDateTimeConverter}}" />
            </core:SfTextInputLayout>

            <core:SfTextInputLayout Hint="Time"
                                            Style="{StaticResource PatientInput}"
                                            HelperText="Recording time">
                <TimePicker Time="{Binding RecordingTime}" />
            </core:SfTextInputLayout>

            <core:SfTextInputLayout Hint="FHR"
                                            Style="{StaticResource PatientInput}"
                                            HelperText="Recording FHR">
                <editors:SfNumericEntry Value="{Binding FHR}" />
            </core:SfTextInputLayout>

            <core:SfTextInputLayout Hint="Contraction"
                                            Style="{StaticResource PatientInput}"
                                            HelperText="Recording contraction">
                <editors:SfNumericEntry Value="{Binding Contraction}" />
            </core:SfTextInputLayout>

            <!--<VerticalStackLayout Padding="10,0,0,0">-->
                <!--<Label Text="What are the FHR &amp; contraction?" FontAttributes="Bold"/>-->

                <!--<buttons:SfRadioGroup x:Name="radioGroup" Margin="30,0,0,0">
                    <buttons:SfRadioButton x:Name="no" Text="No" />
                    <buttons:SfRadioButton x:Name="early" Text="Early"/>
                    <buttons:SfRadioButton x:Name="late" Text="Late" />
                    <buttons:SfRadioButton x:Name="variable" Text="Variable" />
                </buttons:SfRadioGroup>-->
                
            <!--</VerticalStackLayout>-->

            <core:SfTextInputLayout Hint="Additional Notes"
                                            Style="{StaticResource PatientInput}"
                                            HelperText="Any other observations...">
                <Entry Text="{Binding Notes}"
                               Keyboard="Text"
                               ReturnType="Next" />
            </core:SfTextInputLayout>

            <!-- Action Buttons -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="15" Margin="0,20,0,40">
                <Button Grid.Column="0"
                        Text="Cancel"
                        Command="{Binding CancelCommand}"
                        BackgroundColor="{AppThemeBinding Light=#E0E0E0, Dark=#424242}"
                        TextColor="{AppThemeBinding Light=#333, Dark=White}"
                        HeightRequest="50"
                        CornerRadius="10" />

                <Button Grid.Column="1"
                        Text="Save Assessment"
                        Command="{Binding SaveCommand}"
                        BackgroundColor="#4CAF50"
                        TextColor="White"
                        HeightRequest="50"
                        CornerRadius="10" />
            </Grid>

        </VerticalStackLayout>
    </ScrollView>
</ContentView>
