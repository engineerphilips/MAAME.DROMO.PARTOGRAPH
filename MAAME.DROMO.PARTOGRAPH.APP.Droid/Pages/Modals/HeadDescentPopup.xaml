<?xml version="1.0" encoding="utf-8" ?>
<sfPopup:SfPopup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sfPopup="clr-namespace:Syncfusion.Maui.Popup;assembly=Syncfusion.Maui.Popup"
             xmlns:core="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
             xmlns:editors="clr-namespace:Syncfusion.Maui.Inputs;assembly=Syncfusion.Maui.Inputs"
             x:Class="MAAME.DROMO.PARTOGRAPH.APP.Droid.Pages.Modals.HeadDescentPopup">

    <sfPopup:SfPopup.Resources>
        <ResourceDictionary>
            <!-- Input Styles -->
            <Style x:Key="PatientInput" TargetType="core:SfTextInputLayout">
                <Setter Property="ContainerType" Value="Outlined" />
                <Setter Property="OutlineCornerRadius" Value="8" />
            </Style>
        </ResourceDictionary>
    </sfPopup:SfPopup.Resources>
    <sfPopup:SfPopup.HeaderTemplate>
        <DataTemplate>
            <StackLayout>
                <Label Text="Head Descent" VerticalOptions="Center" Margin="15,15,0,0" />
            </StackLayout>
        </DataTemplate>
    </sfPopup:SfPopup.HeaderTemplate>
    <sfPopup:SfPopup.ContentTemplate>
        <DataTemplate>
            <ScrollView>
                <VerticalStackLayout Padding="15,10" Spacing="10">
                    <core:SfTextInputLayout Hint="Date"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Recording time">
                        <DatePicker Date="{Binding RecordingDate}" />
                    </core:SfTextInputLayout>

                    <core:SfTextInputLayout Hint="Time"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Recording time">
                        <TimePicker Time="{Binding RecordingTime}" />
                    </core:SfTextInputLayout>

                    <core:SfTextInputLayout Hint="Head descent (fifths palpable)"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Descent 0-5 (5/5 high, 0/5 fully descended)">
                        <editors:SfNumericEntry Value="{Binding Station}"
                                               Minimum="0" Maximum="{Binding MaximumDescent}"
                                               CustomFormat="0"
                                               ValueChangeMode="OnKeyFocus" />
                    </core:SfTextInputLayout>

                    <!-- Validation Message -->
                    <Frame IsVisible="{Binding ShowValidationMessage}"
                           BackgroundColor="#FFEBEE"
                           BorderColor="#EF5350"
                           CornerRadius="8"
                           Padding="10"
                           Margin="0,0,0,10">
                        <Label Text="{Binding ValidationMessage}"
                               TextColor="#C62828"
                               FontSize="12" />
                    </Frame>

                    <!-- Measurement History Section -->
                    <Frame IsVisible="{Binding HasHistory}"
                           BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#2C2C2C}"
                           BorderColor="{AppThemeBinding Light=#E0E0E0, Dark=#424242}"
                           CornerRadius="10"
                           Padding="12"
                           Margin="0,5,0,10">
                        <VerticalStackLayout Spacing="8">
                            <Label Text="Previous Measurements"
                                   FontAttributes="Bold"
                                   FontSize="14"
                                   TextColor="{AppThemeBinding Light=#333, Dark=White}" />
                            <CollectionView ItemsSource="{Binding MeasurementHistory}"
                                            HeightRequest="120">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid ColumnDefinitions="Auto,*,Auto"
                                              Padding="8,6"
                                              ColumnSpacing="10">
                                            <Frame Grid.Column="0"
                                                   BackgroundColor="#E8F5E9"
                                                   CornerRadius="15"
                                                   Padding="8,4"
                                                   HasShadow="False">
                                                <Label Text="{Binding Value}"
                                                       FontAttributes="Bold"
                                                       FontSize="13"
                                                       TextColor="#2E7D32"
                                                       VerticalOptions="Center" />
                                            </Frame>
                                            <Label Grid.Column="1"
                                                   Text="{Binding DateTimeDisplay}"
                                                   FontSize="12"
                                                   TextColor="{AppThemeBinding Light=#666, Dark=#AAA}"
                                                   VerticalOptions="Center" />
                                            <Label Grid.Column="2"
                                                   Text="{Binding RecordedBy}"
                                                   FontSize="11"
                                                   FontAttributes="Italic"
                                                   TextColor="{AppThemeBinding Light=#888, Dark=#888}"
                                                   VerticalOptions="Center" />
                                        </Grid>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </VerticalStackLayout>
                    </Frame>

                    <core:SfTextInputLayout Hint="Additional Notes"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Any other observations...">
                        <Entry Text="{Binding Notes}"
                           Keyboard="Text"
                           ReturnType="Next" />
                    </core:SfTextInputLayout>

                    <!-- Action Buttons -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="15" Margin="0,20,0,40">
                        <Button Grid.Column="0"
                            Text="Cancel"
                            Command="{Binding CancelCommand}"
                            BackgroundColor="{AppThemeBinding Light=#E0E0E0, Dark=#424242}"
                            TextColor="{AppThemeBinding Light=#333, Dark=White}"
                            HeightRequest="50"
                            CornerRadius="10" />

                        <Button Grid.Column="1"
                            Text="Save Assessment"
                            Command="{Binding SaveCommand}"
                            BackgroundColor="#4CAF50"
                            TextColor="White"
                            HeightRequest="50"
                            CornerRadius="10" />
                    </Grid>

                </VerticalStackLayout>
            </ScrollView>
        </DataTemplate>
    </sfPopup:SfPopup.ContentTemplate>

</sfPopup:SfPopup>
