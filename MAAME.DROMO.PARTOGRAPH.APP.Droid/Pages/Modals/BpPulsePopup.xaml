<?xml version="1.0" encoding="utf-8" ?>
<sfPopup:SfPopup AnimationMode='SlideOnTop' xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sfPopup="clr-namespace:Syncfusion.Maui.Popup;assembly=Syncfusion.Maui.Popup"
             xmlns:core="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
             xmlns:editors="clr-namespace:Syncfusion.Maui.Inputs;assembly=Syncfusion.Maui.Inputs"
             xmlns:converters="clr-namespace:MAAME.DROMO.PARTOGRAPH.APP.Droid.Converters"
             x:Class="MAAME.DROMO.PARTOGRAPH.APP.Droid.Pages.Modals.BpPulsePopup">

    <sfPopup:SfPopup.Resources>
        <ResourceDictionary>
            <Color x:Key="PrimaryBlue">#0078D4</Color>
            <converters:BoolToTextConverter x:Key="BoolToTextConverter" />
            <converters:InvertedBoolConverter x:Key="InvertedBoolConverter" />
            <!-- Input Styles -->
            <Style x:Key="PatientInput" TargetType="core:SfTextInputLayout">
                <Setter Property="ContainerType" Value="Outlined" />
                <Setter Property="OutlineCornerRadius" Value="8" />
                <Setter Property="Stroke" Value="{StaticResource PrimaryBlue}" />
            </Style>
        </ResourceDictionary>
    </sfPopup:SfPopup.Resources>
    <sfPopup:SfPopup.HeaderTemplate>
        <DataTemplate>
            <StackLayout>
                <Label Text="Blood Pressure &amp; Pulse" VerticalOptions="Center" Margin="15,15,0,0" />
            </StackLayout> 
        </DataTemplate>
    </sfPopup:SfPopup.HeaderTemplate>
    <sfPopup:SfPopup.ContentTemplate>
        <DataTemplate>
            <ScrollView>
                <VerticalStackLayout Padding="15,10" Spacing="10">
                    <core:SfTextInputLayout Hint="Date"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Recording time">
                        <DatePicker Date="{Binding RecordingDate, Converter={StaticResource DateOnlyToDateTimeConverter}}" />
                    </core:SfTextInputLayout>

                    <core:SfTextInputLayout Hint="Time"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Recording time">
                        <TimePicker Time="{Binding RecordingTime}" />
                    </core:SfTextInputLayout>

                    <core:SfTextInputLayout Hint="Pulse"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Recording pulse">
                        <editors:SfNumericEntry Value="{Binding Pulse, Mode=TwoWay}"
                                               CustomFormat="0"
                                               ValueChangeMode="OnKeyFocus" />
                    </core:SfTextInputLayout>

                    <core:SfTextInputLayout Hint="Systolic pressure"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Recording systolic pressure">
                        <editors:SfNumericEntry Value="{Binding Systolic, Mode=TwoWay}"
                                               CustomFormat="0"
                                               ValueChangeMode="OnKeyFocus" />
                    </core:SfTextInputLayout>

                    <core:SfTextInputLayout Hint="Diastolic pressure"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Recording diastolic pressure">
                        <editors:SfNumericEntry Value="{Binding Diastolic, Mode=TwoWay}"
                                               CustomFormat="0"
                                               ValueChangeMode="OnKeyFocus" />
                    </core:SfTextInputLayout>

                    <!-- Measurement History Section -->
                    <Frame IsVisible="{Binding HasHistory}"
                           BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#2C2C2C}"
                           BorderColor="{AppThemeBinding Light=#E0E0E0, Dark=#424242}"
                           CornerRadius="10"
                           Padding="12"
                           Margin="0,5,0,10">
                        <VerticalStackLayout Spacing="8">
                            <Label Text="Previous Measurements"
                                   FontAttributes="Bold"
                                   FontSize="14"
                                   TextColor="{AppThemeBinding Light=#333, Dark=White}" />
                            <CollectionView ItemsSource="{Binding MeasurementHistory}"
                                            HeightRequest="120">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid ColumnDefinitions="Auto,*,Auto"
                                              Padding="8,6"
                                              ColumnSpacing="10">
                                            <Frame Grid.Column="0"
                                                   BackgroundColor="#FFEBEE"
                                                   CornerRadius="15"
                                                   Padding="8,4"
                                                   HasShadow="False">
                                                <Label Text="{Binding Value}"
                                                       FontAttributes="Bold"
                                                       FontSize="12"
                                                       TextColor="#C62828"
                                                       VerticalOptions="Center" />
                                            </Frame>
                                            <Label Grid.Column="1"
                                                   Text="{Binding DateTimeDisplay}"
                                                   FontSize="12"
                                                   TextColor="{AppThemeBinding Light=#666, Dark=#AAA}"
                                                   VerticalOptions="Center" />
                                            <Label Grid.Column="2"
                                                   Text="{Binding RecordedBy}"
                                                   FontSize="11"
                                                   FontAttributes="Italic"
                                                   TextColor="{AppThemeBinding Light=#888, Dark=#888}"
                                                   VerticalOptions="Center" />
                                        </Grid>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </VerticalStackLayout>
                    </Frame>

                    <!-- Advanced Fields Toggle -->
                    <!--<Frame BorderColor="{AppThemeBinding Light=#E0E0E0, Dark=#424242}"
                           BackgroundColor="{AppThemeBinding Light=#F5F5F5, Dark=#2C2C2C}"
                           CornerRadius="10"
                           Padding="15,10"
                           Margin="0,10,0,10">
                        <HorizontalStackLayout Spacing="10">
                            <Switch IsToggled="{Binding ShowAdvancedFields}"
                                    VerticalOptions="Center"
                                    OnColor="#4CAF50" />
                            <Label Text="Show Advanced WHO 2020 Fields"
                                   VerticalOptions="Center"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light=#333, Dark=White}" />
                        </HorizontalStackLayout>
                    </Frame>-->

                    <!-- WHO 2020 Advanced Fields Section -->
                    <!--<VerticalStackLayout IsVisible="{Binding ShowAdvancedFields}" Spacing="10">

                        --><!-- WHO 2020 Critical Pre-eclampsia Screening --><!--
                        <Label Text="Pre-eclampsia Screening" FontAttributes="Bold" Margin="0,10,0,5" />

                    <CheckBox IsChecked="{Binding SevereHypertension}" />
                    <Label Text="Severe Hypertension (≥160/110 mmHg)" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding Headache}" />
                    <Label Text="Headache" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding VisualDisturbances}" />
                    <Label Text="Visual Disturbances (blurred vision, spots)" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding EpigastricPain}" />
                    <Label Text="Epigastric Pain / Right Upper Quadrant Pain" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding Hyperreflexia}" />
                    <Label Text="Hyperreflexia / Clonus" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding Edema}" />
                    <Label Text="Edema (face, hands, generalized)" Margin="-30,0,0,0" VerticalOptions="Center" />

                    --><!-- Emergency Response --><!--
                    <Label Text="Clinical Response" FontAttributes="Bold" Margin="0,10,0,5" />

                    <CheckBox IsChecked="{Binding EmergencyProtocolActivated}" />
                    <Label Text="⚠️ Emergency Protocol Activated" Margin="-30,0,0,0" VerticalOptions="Center" TextColor="Red" />

                    <CheckBox IsChecked="{Binding MagnesiumSulfateGiven}" />
                    <Label Text="Magnesium Sulfate Given (seizure prophylaxis)" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding AntihypertensiveGiven}" />
                    <Label Text="Antihypertensive Medication Given" Margin="-30,0,0,0" VerticalOptions="Center" />

                    </VerticalStackLayout>-->
                    <!-- End Advanced Fields Section -->

                    <core:SfTextInputLayout Hint="Additional Notes"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Any other observations...">
                        <Entry Text="{Binding Notes}"
                           Keyboard="Text"
                           ReturnType="Next" />
                    </core:SfTextInputLayout>

                    <!-- Action Buttons -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="15" Margin="0,20,0,40">
                        <Button Grid.Column="0"
                            Text="Cancel"
                            Command="{Binding CancelCommand}"
                            BackgroundColor="{AppThemeBinding Light=#E0E0E0, Dark=#424242}"
                            TextColor="{AppThemeBinding Light=#333, Dark=White}"
                            HeightRequest="50"
                            CornerRadius="10" />

                        <Button Grid.Column="1"
                            Text="{Binding IsBusy, Converter={StaticResource BoolToTextConverter}, ConverterParameter='Saving...|Save Assessment'}"
                            Command="{Binding SaveCommand}"
                            IsEnabled="{Binding IsBusy, Converter={StaticResource InvertedBoolConverter}}"
                            BackgroundColor="{StaticResource PrimaryBlue}"
                            TextColor="White"
                            HeightRequest="50"
                            CornerRadius="10" />
                    </Grid>

                </VerticalStackLayout>
            </ScrollView>
        </DataTemplate>
    </sfPopup:SfPopup.ContentTemplate>

</sfPopup:SfPopup>

