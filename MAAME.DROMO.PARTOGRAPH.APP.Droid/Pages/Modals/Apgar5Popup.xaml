<?xml version="1.0" encoding="utf-8" ?>
<sfPopup:SfPopup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sfPopup="clr-namespace:Syncfusion.Maui.Popup;assembly=Syncfusion.Maui.Popup"
             xmlns:core="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
             xmlns:editors="clr-namespace:Syncfusion.Maui.Inputs;assembly=Syncfusion.Maui.Inputs"
             x:Class="MAAME.DROMO.PARTOGRAPH.APP.Droid.Pages.Modals.Apgar5Popup">

    <sfPopup:SfPopup.Resources>
        <ResourceDictionary>
            <!-- Input Styles -->
            <Style x:Key="PatientInput" TargetType="core:SfTextInputLayout">
                <Setter Property="ContainerType" Value="Outlined" />
                <Setter Property="OutlineCornerRadius" Value="8" />
            </Style>
            <Style x:Key="SectionTitle" TargetType="Label">
                <Setter Property="FontSize" Value="16" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="Margin" Value="0,10,0,5" />
            </Style>
        </ResourceDictionary>
    </sfPopup:SfPopup.Resources>

    <sfPopup:SfPopup.HeaderTemplate>
        <DataTemplate>
            <StackLayout>
                <Label Text="APGAR Score - 5 Minutes"
                       VerticalOptions="Center"
                       FontSize="18"
                       FontAttributes="Bold"
                       Margin="15,15,0,0" />
                <Label Text="Score each component (0-2 points)"
                       VerticalOptions="Center"
                       FontSize="12"
                       TextColor="Gray"
                       Margin="15,5,0,10" />
            </StackLayout>
        </DataTemplate>
    </sfPopup:SfPopup.HeaderTemplate>

    <sfPopup:SfPopup.ContentTemplate>
        <DataTemplate>
            <ScrollView>
                <VerticalStackLayout Padding="15,10" Spacing="15">

                    <!-- Total Score Display -->
                    <Border BackgroundColor="{Binding TotalScoreColor}"
                            StrokeThickness="0"
                            Padding="15"
                            Margin="0,0,0,10">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="10"/>
                        </Border.StrokeShape>
                        <VerticalStackLayout Spacing="5">
                            <Label Text="Total APGAR Score"
                                   FontSize="14"
                                   TextColor="White"
                                   HorizontalOptions="Center" />
                            <Label Text="{Binding TotalScore}"
                                   FontSize="32"
                                   FontAttributes="Bold"
                                   TextColor="White"
                                   HorizontalOptions="Center" />
                            <Label Text="{Binding ScoreInterpretation}"
                                   FontSize="12"
                                   TextColor="White"
                                   HorizontalOptions="Center" />
                        </VerticalStackLayout>
                    </Border>

                    <!-- Heart Rate -->
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Heart Rate" Style="{StaticResource SectionTitle}" />
                        <editors:SfSegmentedControl
                            SelectedIndex="{Binding HeartRate}"
                            SegmentHeight="60"
                            CornerRadius="8"
                            VisibleSegmentsCount="3"
                            SelectionIndicatorSettings="{editors:SelectionIndicatorSettings Background=#2196F3, StrokeThickness=2, Stroke=#1976D2}">
                            <editors:SfSegmentedControl.ItemsSource>
                                <x:Array Type="{x:Type x:String}">
                                    <x:String>0&#10;Absent</x:String>
                                    <x:String>1&#10;&lt; 100 bpm</x:String>
                                    <x:String>2&#10;≥100 bpm</x:String>
                                </x:Array>
                            </editors:SfSegmentedControl.ItemsSource>
                        </editors:SfSegmentedControl>
                    </VerticalStackLayout>

                    <!-- Respiratory Effort -->
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Respiratory Effort" Style="{StaticResource SectionTitle}" />
                        <editors:SfSegmentedControl
                            SelectedIndex="{Binding RespiratoryEffort}"
                            SegmentHeight="60"
                            CornerRadius="8"
                            VisibleSegmentsCount="3"
                            SelectionIndicatorSettings="{editors:SelectionIndicatorSettings Background=#2196F3, StrokeThickness=2, Stroke=#1976D2}">
                            <editors:SfSegmentedControl.ItemsSource>
                                <x:Array Type="{x:Type x:String}">
                                    <x:String>0&#10;Absent</x:String>
                                    <x:String>1&#10;Slow/Irregular</x:String>
                                    <x:String>2&#10;Good/Crying</x:String>
                                </x:Array>
                            </editors:SfSegmentedControl.ItemsSource>
                        </editors:SfSegmentedControl>
                    </VerticalStackLayout>

                    <!-- Muscle Tone -->
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Muscle Tone" Style="{StaticResource SectionTitle}" />
                        <editors:SfSegmentedControl
                            SelectedIndex="{Binding MuscleTone}"
                            SegmentHeight="60"
                            CornerRadius="8"
                            VisibleSegmentsCount="3"
                            SelectionIndicatorSettings="{editors:SelectionIndicatorSettings Background=#2196F3, StrokeThickness=2, Stroke=#1976D2}">
                            <editors:SfSegmentedControl.ItemsSource>
                                <x:Array Type="{x:Type x:String}">
                                    <x:String>0&#10;Limp</x:String>
                                    <x:String>1&#10;Some Flexion</x:String>
                                    <x:String>2&#10;Active Motion</x:String>
                                </x:Array>
                            </editors:SfSegmentedControl.ItemsSource>
                        </editors:SfSegmentedControl>
                    </VerticalStackLayout>

                    <!-- Reflex Irritability -->
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Reflex Irritability" Style="{StaticResource SectionTitle}" />
                        <editors:SfSegmentedControl
                            SelectedIndex="{Binding ReflexIrritability}"
                            SegmentHeight="60"
                            CornerRadius="8"
                            VisibleSegmentsCount="3"
                            SelectionIndicatorSettings="{editors:SelectionIndicatorSettings Background=#2196F3, StrokeThickness=2, Stroke=#1976D2}">
                            <editors:SfSegmentedControl.ItemsSource>
                                <x:Array Type="{x:Type x:String}">
                                    <x:String>0&#10;No Response</x:String>
                                    <x:String>1&#10;Grimace</x:String>
                                    <x:String>2&#10;Cry/Sneeze</x:String>
                                </x:Array>
                            </editors:SfSegmentedControl.ItemsSource>
                        </editors:SfSegmentedControl>
                    </VerticalStackLayout>

                    <!-- Color -->
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Color" Style="{StaticResource SectionTitle}" />
                        <editors:SfSegmentedControl
                            SelectedIndex="{Binding Color}"
                            SegmentHeight="60"
                            CornerRadius="8"
                            VisibleSegmentsCount="3"
                            SelectionIndicatorSettings="{editors:SelectionIndicatorSettings Background=#2196F3, StrokeThickness=2, Stroke=#1976D2}">
                            <editors:SfSegmentedControl.ItemsSource>
                                <x:Array Type="{x:Type x:String}">
                                    <x:String>0&#10;Blue/Pale</x:String>
                                    <x:String>1&#10;Body Pink</x:String>
                                    <x:String>2&#10;Completely Pink</x:String>
                                </x:Array>
                            </editors:SfSegmentedControl.ItemsSource>
                        </editors:SfSegmentedControl>
                    </VerticalStackLayout>

                    <!-- Action Buttons -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="15" Margin="0,20,0,20">
                        <Button Grid.Column="0"
                            Text="Cancel"
                            Command="{Binding CancelCommand}"
                            BackgroundColor="{AppThemeBinding Light=#E0E0E0, Dark=#424242}"
                            TextColor="{AppThemeBinding Light=#333, Dark=White}"
                            HeightRequest="50"
                            CornerRadius="10" />

                        <Button Grid.Column="1"
                            Text="Save Score"
                            Command="{Binding SaveCommand}"
                            BackgroundColor="#4CAF50"
                            TextColor="White"
                            HeightRequest="50"
                            CornerRadius="10" />
                    </Grid>

                </VerticalStackLayout>
            </ScrollView>
        </DataTemplate>
    </sfPopup:SfPopup.ContentTemplate>

</sfPopup:SfPopup>
