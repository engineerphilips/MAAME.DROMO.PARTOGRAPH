<?xml version="1.0" encoding="utf-8" ?>
<sfPopup:SfPopup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sfPopup="clr-namespace:Syncfusion.Maui.Popup;assembly=Syncfusion.Maui.Popup"
             xmlns:core="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
             xmlns:buttons="clr-namespace:Syncfusion.Maui.Buttons;assembly=Syncfusion.Maui.Buttons"
             xmlns:editors="clr-namespace:Syncfusion.Maui.Inputs;assembly=Syncfusion.Maui.Inputs"
             x:Class="MAAME.DROMO.PARTOGRAPH.APP.Droid.Pages.Modals.UrinePopup">

    <sfPopup:SfPopup.Resources>
        <ResourceDictionary>
            <!-- Input Styles -->
            <Style x:Key="PatientInput" TargetType="core:SfTextInputLayout">
                <Setter Property="ContainerType" Value="Outlined" />
                <Setter Property="OutlineCornerRadius" Value="8" />
            </Style>
        </ResourceDictionary>
    </sfPopup:SfPopup.Resources>
    <sfPopup:SfPopup.HeaderTemplate>
        <DataTemplate>
            <StackLayout>
                <Label Text="Urine" VerticalOptions="Center" Margin="15,15,0,0" />
            </StackLayout>
        </DataTemplate>
    </sfPopup:SfPopup.HeaderTemplate>
    <sfPopup:SfPopup.ContentTemplate>
        <DataTemplate>
            <ScrollView>
                <VerticalStackLayout Padding="15,10" Spacing="10">
                    <core:SfTextInputLayout Hint="Date"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Recording time">
                        <DatePicker Date="{Binding RecordingDate}" />
                    </core:SfTextInputLayout>

                    <core:SfTextInputLayout Hint="Time"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Recording time">
                        <TimePicker Time="{Binding RecordingTime}" />
                    </core:SfTextInputLayout>

                    <VerticalStackLayout Padding="10,0,0,0">
                        <Label Text="What is the protien content in the urine?" FontAttributes="Bold"/>
                    </VerticalStackLayout>

                    <buttons:SfSegmentedControl x:Name="protienSegmentedControl" CornerRadius="8" Margin="-10,0,0,0" VisibleSegmentsCount="5" SelectedIndex="{Binding ProteinIndex, Mode=TwoWay}">
                        <buttons:SfSegmentedControl.ItemsSource>
                            <x:Array Type="{x:Type x:String}">
                                <x:String>P-</x:String>
                                <x:String>P</x:String>
                                <x:String>P1+</x:String>
                                <x:String>P2+</x:String>
                                <x:String>P3+</x:String>
                            </x:Array>
                        </buttons:SfSegmentedControl.ItemsSource>
                    </buttons:SfSegmentedControl>

                    <VerticalStackLayout Padding="10,0,0,0">
                        <Label Text="What is the acetone content in the urine?" FontAttributes="Bold"/>
                    </VerticalStackLayout>

                    <buttons:SfSegmentedControl x:Name="acetoneSegmentedControl" CornerRadius="8" Margin="-10,0,0,0" VisibleSegmentsCount="5" SelectedIndex="{Binding AcetoneIndex, Mode=TwoWay}">
                        <buttons:SfSegmentedControl.ItemsSource>
                            <x:Array Type="{x:Type x:String}">
                                <x:String>A-</x:String>
                                <x:String>A</x:String>
                                <x:String>A1+</x:String>
                                <x:String>A2+</x:String>
                                <x:String>A3+</x:String>
                            </x:Array>
                        </buttons:SfSegmentedControl.ItemsSource>
                    </buttons:SfSegmentedControl>

                    <!-- WHO 2020 Critical Urine Assessment -->
                    <Label Text="Urine Output & Oliguria" FontAttributes="Bold" Margin="0,10,0,5" />

                    <core:SfTextInputLayout Hint="Output Volume (ml)"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Urine output in ml">
                        <editors:SfNumericEntry Value="{Binding OutputMl, Mode=TwoWay}" Minimum="0"
                                               CustomFormat="0"
                                               ValueChangeMode="OnKeyFocus" />
                    </core:SfTextInputLayout>

                    <CheckBox IsChecked="{Binding Oliguria}" />
                    <Label Text="⚠️ Oliguria (<30 ml/hour)" Margin="-30,0,0,0" VerticalOptions="Center" TextColor="OrangeRed" />

                    <CheckBox IsChecked="{Binding Anuria}" />
                    <Label Text="⚠️ Anuria (No urine output)" Margin="-30,0,0,0" VerticalOptions="Center" TextColor="Red" />

                    <!-- Pre-eclampsia Monitoring -->
                    <Label Text="Pre-eclampsia Monitoring" FontAttributes="Bold" Margin="0,10,0,5" />

                    <CheckBox IsChecked="{Binding ProteinuriaNewOnset}" />
                    <Label Text="⚠️ New-Onset Proteinuria" Margin="-30,0,0,0" VerticalOptions="Center" TextColor="OrangeRed" />

                    <CheckBox IsChecked="{Binding ProteinuriaWorsening}" />
                    <Label Text="Worsening Proteinuria" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding LaboratorySampleSent}" />
                    <Label Text="Laboratory Sample Sent (Protein/Creatinine Ratio)" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <!-- Fluid Balance -->
                    <Label Text="Fluid Balance" FontAttributes="Bold" Margin="0,10,0,5" />

                    <core:SfTextInputLayout Hint="Total IV Intake (ml)"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Total intravenous fluid intake">
                        <editors:SfNumericEntry Value="{Binding TotalIVIntakeMl, Mode=TwoWay}" Minimum="0"
                                               CustomFormat="0"
                                               ValueChangeMode="OnKeyFocus" />
                    </core:SfTextInputLayout>

                    <!-- Catheterization -->
                    <Label Text="Bladder Management" FontAttributes="Bold" Margin="0,10,0,5" />

                    <CheckBox IsChecked="{Binding UrinaryRetention}" />
                    <Label Text="Urinary Retention" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <CheckBox IsChecked="{Binding CatheterizationPerformed}" />
                    <Label Text="Catheterization Performed" Margin="-30,0,0,0" VerticalOptions="Center" />

                    <core:SfTextInputLayout Hint="Additional Notes"
                                        Style="{StaticResource PatientInput}"
                                        HelperText="Any other observations...">
                        <Entry Text="{Binding Notes}"
                           Keyboard="Text"
                           ReturnType="Next" />
                    </core:SfTextInputLayout>

                    <!-- Action Buttons -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="15" Margin="0,20,0,40">
                        <Button Grid.Column="0"
                            Text="Cancel"
                            Command="{Binding CancelCommand}"
                            BackgroundColor="{AppThemeBinding Light=#E0E0E0, Dark=#424242}"
                            TextColor="{AppThemeBinding Light=#333, Dark=White}"
                            HeightRequest="50"
                            CornerRadius="10" />

                        <Button Grid.Column="1"
                            Text="Save Assessment"
                            Command="{Binding SaveCommand}"
                            BackgroundColor="#4CAF50"
                            TextColor="White"
                            HeightRequest="50"
                            CornerRadius="10" />
                    </Grid>

                </VerticalStackLayout>
            </ScrollView>
        </DataTemplate>
    </sfPopup:SfPopup.ContentTemplate>

</sfPopup:SfPopup>
